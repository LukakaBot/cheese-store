{
  "devDependencies": {
    "vitepress": "1.0.0-rc.20"
  },
  "scripts": {
    "dev": "pnpm docs:dev --host",
    "build": "pnpm docs:build",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "dependencies": {
    "vue": "^3.3.4"
  }
}